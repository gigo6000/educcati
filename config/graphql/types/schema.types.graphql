type Query {
  course(id: ID!): Course
}

type Course {
  id: ID!
  title: String!
  isPublished: Boolean
  sections(
     id: ID
  ): [Section]
}

type Section {
  id: ID!
  title: String!
  lessons (
      id: ID
  ): [Lesson]
}

type Lesson {
  id: ID!
  title: String!
  slug: String,
  video: String,
  duration: String,
  isCompleted: Boolean,
  isPublished: Boolean
}

